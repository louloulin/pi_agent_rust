//! Phase-2 placeholder mirroring `@earendil-works/pi-evals`: the
//! evaluation harness. Currently empty — the Rust port's conformance
//! tests live in `pi-conformance` and `crates/pi/tests/`. Future
//! Phase-2 rounds may move them under this aggregator.

#![forbid(unsafe_code)]
