[package]
name = "pi-glob-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Glob matching and ignore-control parsing primitives for Pi"

[lints]
workspace = true

[dependencies]
globset = { workspace = true }
ignore = { workspace = true }
regex = { workspace = true }
