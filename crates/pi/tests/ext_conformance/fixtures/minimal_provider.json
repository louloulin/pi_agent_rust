{
  "schema": "pi.ext.scenario_fixture.v1",
  "extension": {
    "id": "minimal_provider",
    "source": {
      "path": "tests/ext_conformance/artifacts/base_fixtures/minimal_provider/index.ts"
    }
  },
  "scenarios": [
    {
      "id": "scn-minimal-provider-001",
      "kind": "provider",
      "summary": "Registers a mock provider",
      "provider_id": "mock-provider",
      "expect": {
        "api": "openai-responses",
        "api_key_env": "env:MOCK_KEY"
      }
    },
    {
      "id": "scn-minimal-provider-neg-001",
      "kind": "provider",
      "summary": "Provider exposes the mock model",
      "provider_id": "mock-provider",
      "expect": {
        "models_contains": [
          "mock-model"
        ]
      }
    }
  ]
}
