[package]
name = "pi-delight"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "Pure presentation helpers for Pi"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
