[package]
name = "pi-index-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Pure query, inverted-index, and relevance primitives for Pi search indexes"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
