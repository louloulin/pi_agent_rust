[package]
name = "pi-markdown-rich"
description = "Markdown enrichment (LaTeX→Unicode, hex swatches, mermaid framing, OSC-8 links) — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
