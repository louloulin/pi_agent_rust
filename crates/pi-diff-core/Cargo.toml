[package]
name = "pi-diff-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Pure text diff parsing, rendering, and hunk composition for Pi"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
similar = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
