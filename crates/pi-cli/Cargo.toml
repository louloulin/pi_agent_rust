[package]
name = "pi-cli"
description = "Clap derive for the `pi` CLI argument surface (Stage 2 leaf crate)"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[features]
# Stub features for forward compatibility — pi-cli's CLI surface flags some
# fields behind these features (matching the legacy meta-crate). Each
# downstream consumer is expected to enable them as needed.
ftui = []
tui = []

[dependencies]
clap = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }