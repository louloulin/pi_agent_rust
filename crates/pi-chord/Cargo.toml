[package]
name = "pi-chord"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 aggregator mirroring @earendil-works/chord: application composition runtime (services, replicated state, RPC, plugins, hostcall lanes)"

[lints]
workspace = true

[dependencies]
pi-hostcall-superinstructions = { workspace = true }
pi-hostcall-io-uring-lane = { workspace = true }
pi-hostcall-s3-fifo = { workspace = true }
pi-buffer-shim = { workspace = true }
pi-http-shim = { workspace = true }
pi-file-lock = { workspace = true }
