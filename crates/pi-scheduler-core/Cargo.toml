[package]
name = "pi-scheduler-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Pure deterministic scheduler state machine and event-loop core for PiJS"

[lints]
workspace = true

[dependencies]
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
