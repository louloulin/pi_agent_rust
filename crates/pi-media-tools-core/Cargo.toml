[package]
name = "pi-media-tools-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "Pure media asset and transform primitives for Pi"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
