//! Swarm activity ledger core algorithms.
//!
//! The implementation lives in `pi-swarm-ledger-core`; this compatibility
//! module keeps the existing `pi::swarm_activity_ledger` API stable while
//! runtime and UI integrations remain in the coding-agent crate.

pub use pi_swarm_ledger_core::*;
