[package]
name = "pi-theme-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Pure theme models, color parsing, validation, and terminal detection for Pi"

[lints]
workspace = true

[dependencies]
pi-error = { workspace = true }
serde = { workspace = true }
