[package]
name = "pi-agent-cx"
description = "Capability-scoped async context wrapper (AgentCx + AgentFs / AgentTime / AgentProcess) — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
asupersync = { workspace = true }
