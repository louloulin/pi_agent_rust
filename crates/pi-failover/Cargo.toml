[package]
name = "pi-failover"
description = "Cross-model failover: failure classification, credential rotation, path-scoped model sets (Stage 2 leaf crate)"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
pi-error = { workspace = true }
pi-provider-metadata = { workspace = true }
serde = { workspace = true }
