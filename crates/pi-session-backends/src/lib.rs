//! Phase-2 placeholder mirroring `@earendil-works/pi-session-backends`:
//! session persistence backends (SQLite sidecar, JSONL tree, picker,
//! store-v2). The actual code is still inlined in `crates/pi/src`; a
//! future Phase-2 round will extract a `pi-session` leaf and re-export
//! it here.

#![forbid(unsafe_code)]
