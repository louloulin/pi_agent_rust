[package]
name = "pi-client"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 aggregator mirroring @earendil-works/pi-client: transport-neutral client for remote pi sessions over framed bytes"

[lints]
workspace = true

[dependencies]
pi-web-remote = { workspace = true }
