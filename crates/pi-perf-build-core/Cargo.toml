[package]
name = "pi-perf-build-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Pure performance-build metrics and configuration for Pi"
license.workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }

[lints]
workspace = true
