[package]
name = "pi-overlay-system"
description = "Unified overlay and set-piece surfaces (Esc-stack, focus-trap, toasts, picker modals) — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
