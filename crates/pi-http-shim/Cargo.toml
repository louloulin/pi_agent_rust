[package]
name = "pi-http-shim"
description = "Node.js `http`/`https` virtual module JS source for the extension runtime (Stage 2 leaf crate)"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true
