[package]
name = "pi-secrets-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Backend-neutral secret references, scopes, and encrypted envelopes for Pi"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
