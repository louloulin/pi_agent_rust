[package]
name = "pi-file-lock"
description = "Cross-process advisory file locking (Stage 2 leaf crate)"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
filetime = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[target.'cfg(unix)'.dependencies]
rustix = { workspace = true }
