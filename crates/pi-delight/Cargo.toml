[package]
name = "pi-delight"
description = "Premium delight layer (shimmer sweeps, sparklines, fireworks particles, terminal titles) — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
