[package]
name = "pi-token-count"
description = "BPE token counting surface (HeuristicCounter + optional tiktoken-backed BpeCounter) — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
tiktoken-rs = { workspace = true, optional = true }

[features]
bpe-tokens = ["dep:tiktoken-rs"]
