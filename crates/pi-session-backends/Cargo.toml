[package]
name = "pi-session-backends"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 module mirroring @earendil-works/pi-session-backends: session persistence backends (SQLite, JSONL) — currently still in crates/pi-coding-agent/src"

[lints]
workspace = true

[dependencies]
# Round 18: pi-session-backends was an empty placeholder. All session
# / compaction / sqlite modules moved to pi-coding-agent to break the
# pi-coding-agent ↔ pi-session-backends cycle.
#
# Round 26: re-housing leaves here, one at a time, to restore the upstream
# surface. Each leaf gets its own dep list appended below.
#
# compaction_snap.rs (Round 26.1)
anyhow = { workspace = true }
pi-ai = { workspace = true }
base64 = { workspace = true }
flate2 = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
sha2 = { workspace = true }
chrono = { workspace = true }
pi-error = { workspace = true }


# session_import.rs (Round 45) depends only on SessionImportFactory/SessionImportSink
# traits; the native Session adapter remains in pi-coding-agent.

[dev-dependencies]
# tests/session_persistence.rs (Round 26.2 integration test)
asupersync = { workspace = true }
pi-coding-agent = { workspace = true }
tempfile = { workspace = true }
