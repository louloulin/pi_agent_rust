[package]
name = "pi-worktree-iso"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Workspace isolation for subagents: git-worktree materialization + serialized parent apply"

[lints]
workspace = true

[dependencies]
pi-error = { workspace = true }
serde = { workspace = true }
ignore = { workspace = true }

[dev-dependencies]
filetime = { workspace = true }
