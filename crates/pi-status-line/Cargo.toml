[package]
name = "pi-status-line"
description = "Status line presets + segment composition — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }

[features]
# Forwarded from `pi`'s `tui` feature so the same `#[cfg(feature = "tui")]`
# guards in the source keep their semantics inside the leaf.
tui = []
