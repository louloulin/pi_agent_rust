[package]
name = "pi-buffer-shim"
description = "Node.js Buffer shim JS source for the QuickJS extension runtime — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true
