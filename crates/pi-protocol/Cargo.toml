[package]
name = "pi-protocol"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 placeholder mirroring @earendil-works/pi-protocol. Round 18 absorbed all rpc / acp / sdk / jsonrpc / validation_broker modules into pi-coding-agent to break the dependency cycle."

[lints]
workspace = true

[dependencies]
# Round 18: pi-protocol is now an empty placeholder. All protocol files
# moved to pi-coding-agent to break the pi-coding-agent ↔ pi-protocol
# cycle.
anyhow = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }