[package]
name = "pi-provider-metadata"
description = "Canonical provider registry: ids, aliases, auth env keys, routing defaults (Stage 2 leaf crate)"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
url = { workspace = true }
