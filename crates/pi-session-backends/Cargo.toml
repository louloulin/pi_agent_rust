[package]
name = "pi-session-backends"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 placeholder mirroring @earendil-works/pi-session-backends: session persistence backends (SQLite, JSONL) — currently still in crates/pi/src"

[lints]
workspace = true

[dependencies]
# Round 18: pi-session-backends is now an empty placeholder. All session
# / compaction / sqlite modules moved to pi-coding-agent to break the
# pi-coding-agent ↔ pi-session-backends cycle.
anyhow = { workspace = true }
