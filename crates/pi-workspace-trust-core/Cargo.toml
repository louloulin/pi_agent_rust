[package]
name = "pi-workspace-trust-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "Pure workspace trust decision algorithms for Pi"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
