{
  "version": "1.0",
  "tool": "jobs",
  "description": "JobsTool contract for bd-cv653.3.10 and bd-cv653.8.1: list envelope, required job identity, and explicit action taxonomy.",
  "cases": [
    {
      "name": "jobs_list_returns_session_registry",
      "description": "list returns the versioned background-job registry whether it is empty or populated by another case in the process",
      "requirement_ids": [
        "JOBS-LIST-1"
      ],
      "input": {
        "action": "list"
      },
      "expected": {
        "content_regex": "^(No background jobs this session\\.|[0-9]+ background job\\(s\\):)",
        "details_exact": {
          "schema": "pi.bash_job.v1"
        }
      }
    },
    {
      "name": "jobs_wait_requires_job_id",
      "description": "wait refuses an ambiguous request before entering a wait loop",
      "requirement_ids": [
        "JOBS-WAIT-1"
      ],
      "input": {
        "action": "wait",
        "timeoutMs": 1
      },
      "expect_error": true,
      "error_contains": "jobs wait requires jobId",
      "expected": {}
    },
    {
      "name": "jobs_rejects_unknown_action",
      "description": "unknown actions fail with the complete accepted action set",
      "requirement_ids": [
        "JOBS-ERR-1"
      ],
      "input": {
        "action": "detach"
      },
      "expect_error": true,
      "error_contains": "Unknown jobs action 'detach'; expected list, wait, or cancel",
      "expected": {}
    }
  ]
}
