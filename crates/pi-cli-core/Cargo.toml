[package]
name = "pi-cli-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "Command parsing, validation, and help generation for Pi"
license-file = "../../LICENSE"
repository = "https://github.com/louloulin/pi_agent_rust"

[features]
default = []
ftui = []

[dependencies]
clap = { version = "4.5.60", features = ["derive", "env", "string"] }
