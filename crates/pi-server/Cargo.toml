[package]
name = "pi-server"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 placeholder mirroring @earendil-works/pi-server. Round 18 absorbed all server modules into pi-coding-agent to break the dependency cycle."

[lints]
workspace = true

[dependencies]
# Round 18: pi-server is now an empty placeholder. All server files moved
# to pi-coding-agent to break the pi-coding-agent ↔ pi-server cycle.
anyhow = { workspace = true }