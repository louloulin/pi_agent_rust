[package]
name = "pi-magic-keywords"
description = "Magic keyword detection + session telemetry sink trait (bd-cv653.3.6) — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
