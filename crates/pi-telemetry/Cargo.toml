[package]
name = "pi-telemetry"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 aggregator mirroring @earendil-works/pi-telemetry: vendor-neutral telemetry contracts, profiler, PMU, and session metrics"

[lints]
workspace = true

[dependencies]
pi-pmu-telemetry = { workspace = true }
pi-profiler = { workspace = true }
pi-session-metrics = { workspace = true }
