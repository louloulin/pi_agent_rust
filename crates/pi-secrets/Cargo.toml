[package]
name = "pi-secrets"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Credential-shape detection and session-scoped secrets vault for Pi"

[lints]
workspace = true

[dependencies]
pi-error = { workspace = true }
regex = { workspace = true }
serde = { workspace = true }
