[package]
name = "pi-tui"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 aggregator mirroring @earendil-works/pi-tui: Terminal UI rendering and input handling"

[lints]
workspace = true

[dependencies]
# TUI is currently fully inlined in crates/pi (interactive_ftui.rs,
# interactive/agent.rs, interactive/commands.rs, tui.rs, keybindings.rs,
# theme.rs). This aggregator is a placeholder; future rounds will move
# the UI code into a pi-ui core leaf crate and re-export it here.
