[package]
name = "pi-extension-inclusion"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Final inclusion list generation for Pi extension candidates: merges scoring tiers, candidate pool provenance, license verdicts, and validation evidence into an authoritative inclusion list with version pins"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
