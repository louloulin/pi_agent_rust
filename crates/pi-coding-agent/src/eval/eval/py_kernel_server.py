#!/usr/bin/env python3
"""Round 19 stub: the Python eval kernel server was not ported from the
upstream `@earendil-works/pi` package tree. The eval pipeline currently
remains Rust-only; this stub keeps the `include_str!` macro in
`crates/pi-coding-agent/src/eval/eval.rs` satisfiable until the Python
kernel is implemented.
"""

if __name__ == "__main__":
    print("py_kernel_server.py is a stub; the Python eval kernel is not yet ported")