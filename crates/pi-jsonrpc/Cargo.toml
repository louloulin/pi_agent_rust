[package]
name = "pi-jsonrpc"
description = "JSON-RPC 2.0 framing primitives shared by LSP, DAP, and MCP transports (Stage 2 leaf crate)"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
