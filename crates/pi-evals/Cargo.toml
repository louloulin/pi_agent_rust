[package]
name = "pi-evals"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Phase-2 placeholder mirroring @earendil-works/pi-evals. Round 18 absorbed all eval modules into pi-coding-agent to break the dependency cycle."

[lints]
workspace = true

[dependencies]
# Round 18: pi-evals is now an empty placeholder. All eval files moved
# to pi-coding-agent to break the pi-coding-agent ↔ pi-evals cycle.
anyhow = { workspace = true }