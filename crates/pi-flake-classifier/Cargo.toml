[package]
name = "pi-flake-classifier"
description = "Conformance flake classifier (bd-k5q5.5.4) — classify test failures as transient or deterministic — Stage 2 leaf crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
